# Lot P1 — corrections prioritaires — Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Corriger trois points de la revue du 2026-07-11 : confirmation avant suppression d'un deck, miniatures + survol plein format dans le volet deck, et gestion d'erreur à l'upload d'un dos de carte.

**Architecture:** SPA React statique (Vite). Tout se joue côté front : `web/src/lib/lang.js` (helper pur, testé Vitest), `web/src/lib/i18n.js` (clés fr/en, parité testée), et trois composants (`CardBrowser`, `DeckPanel`, `DeckManager`, `ExportDialog`) + un nouveau module partagé `CardPreview.jsx`. Aucun changement de format d'export ni de schéma de données.

**Tech Stack:** React 18, Vite 5, Vitest 2. Pas de React Testing Library dans le repo → le comportement des composants est vérifié manuellement via `npm run dev` (:5173) ; seules les fonctions pures de `lib/` sont testées en Vitest.

**Référence spec:** `docs/superpowers/specs/2026-07-11-p1-fixes-design.md`

---

## File structure

```
web/src/lib/lang.js                  # MODIF : + deckThumbWidth() (pur, testé)
web/src/lib/i18n.js                  # MODIF : + 2 clés (fr+en)
web/src/components/CardPreview.jsx   # NOUVEAU : useCardPreview() + <CardPreview>
web/src/components/CardBrowser.jsx   # MODIF : consomme le module partagé
web/src/components/DeckPanel.jsx     # MODIF : miniatures + survol plein format
web/src/components/DeckManager.jsx   # MODIF : confirmation suppression inline
web/src/components/ExportDialog.jsx  # MODIF : try/catch upload dos
test/lang.test.js                    # MODIF : + bloc deckThumbWidth
```

Ordre des tâches : 1 (helper pur) → 2 (extraction preview, prérequis de 3) → 3 (volet deck) → 4 (confirmation suppression) → 5 (erreur upload). Les tâches 4 et 5 sont indépendantes des autres.

---

### Task 1: `deckThumbWidth` — largeur de miniature quantifiée (TDD)

**Files:**
- Modify: `web/src/lib/lang.js` (ajout en fin de fichier)
- Test: `test/lang.test.js` (nouveau bloc `describe`)

- [ ] **Step 1: Écrire le test qui échoue**

Ajouter à la fin de `test/lang.test.js` (avant la dernière ligne, à l'intérieur du fichier ; importer aussi `deckThumbWidth`) :

```js
import { deckThumbWidth } from '../web/src/lib/lang.js';

describe('deckThumbWidth', () => {
  it('rounds up to 100px steps', () => {
    expect(deckThumbWidth(285)).toBe(300);
    expect(deckThumbWidth(300)).toBe(300);
    expect(deckThumbWidth(301)).toBe(400);
  });
  it('floors at 200px', () => {
    expect(deckThumbWidth(85)).toBe(200);
    expect(deckThumbWidth(150)).toBe(200);
  });
  it('caps at the 570px source width', () => {
    expect(deckThumbWidth(570)).toBe(570);
    expect(deckThumbWidth(9999)).toBe(570);
  });
});
```

> Note : l'`import` de `deckThumbWidth` peut être fusionné avec l'import existant en tête de `test/lang.test.js` :
> `import { cardImageSrc, cardImageEn, cardName, cardThumbSrc, deckThumbWidth } from '../web/src/lib/lang.js';`

- [ ] **Step 2: Lancer le test → il échoue**

Run: `npx vitest run test/lang.test.js`
Expected: FAIL — `deckThumbWidth is not a function` (export absent).

- [ ] **Step 3: Implémenter**

Ajouter en fin de `web/src/lib/lang.js` :

```js
// Thumbnail width to request from the proxy for a deck-panel card displayed at
// `cardW` px. Quantized to 100px steps (floor 200, cap 570 = source width) so
// the zoom slider yields at most ~5 distinct, cache-friendly URLs while staying
// >= the on-screen size (crisp at every zoom; pixel-perfect at 100%).
export function deckThumbWidth(cardW) {
  return Math.min(570, Math.max(200, Math.ceil(cardW / 100) * 100));
}
```

- [ ] **Step 4: Lancer le test → il passe**

Run: `npx vitest run test/lang.test.js`
Expected: PASS (tous les blocs, dont `deckThumbWidth`).

- [ ] **Step 5: Commit**

```bash
git add web/src/lib/lang.js test/lang.test.js
git commit -m "feat: add deckThumbWidth helper for quantized deck-panel thumbnails"
```

---

### Task 2: Extraire le survol plein format dans un module partagé

Déplace la logique de preview de `CardBrowser` vers `web/src/components/CardPreview.jsx` (hook + élément), puis refactore `CardBrowser` pour la consommer. Comportement de la grille **strictement identique** après refactoring.

**Files:**
- Create: `web/src/components/CardPreview.jsx`
- Modify: `web/src/components/CardBrowser.jsx` (remplacement complet)

- [ ] **Step 1: Créer le module partagé**

Créer `web/src/components/CardPreview.jsx` avec ce contenu exact :

```jsx
import { useRef, useEffect } from 'react';
import { cardImageSrc, cardImageEn } from '../lib/lang.js';

// Natural source image dimensions (see README). The hover preview shows the
// image at full size, scaled down only if it would overflow the viewport.
const PREVIEW_W = 570;
const PREVIEW_H = 796;

// The preview only appears once the pointer has held still over a card for this
// long, so scrolling/scanning never flashes tooltips.
const PREVIEW_DELAY_MS = 600;

// Shared full-size hover preview for card images, driven imperatively via refs
// so moving the mouse never re-renders the (potentially hundreds of) cells that
// use it. Consumed by both the browser grid and the deck panel.
export function useCardPreview(lang) {
  const previewRef = useRef(null);
  const previewImgRef = useRef(null);
  const timerRef = useRef(null);      // pending "show after idle" timer
  const shownIdRef = useRef(null);    // id of the card currently previewed
  const posRef = useRef({ x: 0, y: 0 });

  function positionPreview(x, y) {
    const box = previewRef.current;
    if (!box) return;
    const scale = Math.min(1, (window.innerHeight * 0.9) / PREVIEW_H);
    const w = PREVIEW_W * scale;
    const h = PREVIEW_H * scale;
    const margin = 16;
    let left = x + margin;
    if (left + w > window.innerWidth - margin) left = x - margin - w;
    if (left < margin) left = margin;
    let top = y - h / 2;
    top = Math.max(margin, Math.min(top, window.innerHeight - h - margin));
    box.style.width = `${w}px`;
    box.style.left = `${left}px`;
    box.style.top = `${top}px`;
  }

  function showPreview(c) {
    const box = previewRef.current;
    const img = previewImgRef.current;
    if (!box || !img) return;
    const en = cardImageEn(c);
    img.onerror = () => { if (img.getAttribute('src') !== en) img.src = en; };
    img.src = cardImageSrc(c, lang);
    box.style.display = 'block';
    shownIdRef.current = c.id;
    positionPreview(posRef.current.x, posRef.current.y);
  }

  function hidePreview() {
    clearTimeout(timerRef.current);
    shownIdRef.current = null;
    const box = previewRef.current;
    if (box) box.style.display = 'none';
  }

  // Show the preview only once the pointer has held still over the same card for
  // PREVIEW_DELAY_MS. Every move to a new card (including cards sliding under a
  // stationary cursor while scrolling) re-arms the timer, so it never flashes.
  function trackPointer(e, c) {
    posRef.current = { x: e.clientX, y: e.clientY };
    if (shownIdRef.current === c.id) {
      positionPreview(e.clientX, e.clientY); // already up: just follow the cursor
      return;
    }
    if (shownIdRef.current !== null) hidePreview(); // a different card was showing
    clearTimeout(timerRef.current);
    timerRef.current = setTimeout(() => showPreview(c), PREVIEW_DELAY_MS);
  }

  // Cancel any pending/shown preview the moment anything scrolls. Scrolling
  // doesn't reliably fire mouse events, so a timer armed while hovering a card
  // before the scroll would otherwise fire over a card no longer under the
  // cursor. capture=true catches scroll from inner containers (scroll events
  // don't bubble). Also drops the timer if we unmount mid-hover.
  useEffect(() => {
    const cancel = () => hidePreview();
    window.addEventListener('wheel', cancel, true);
    window.addEventListener('scroll', cancel, true);
    return () => {
      window.removeEventListener('wheel', cancel, true);
      window.removeEventListener('scroll', cancel, true);
      clearTimeout(timerRef.current);
    };
  }, []);

  return { previewRef, previewImgRef, trackPointer, hidePreview };
}

// The shared preview box element. Render exactly once per component that uses
// the hook, passing the refs it returned.
export function CardPreview({ previewRef, previewImgRef }) {
  return (
    <div className="card-preview" ref={previewRef} style={{ display: 'none' }} aria-hidden="true">
      <img ref={previewImgRef} alt="" />
    </div>
  );
}
```

- [ ] **Step 2: Refactorer `CardBrowser` pour consommer le module**

Remplacer **tout** le contenu de `web/src/components/CardBrowser.jsx` par :

```jsx
import React, { useMemo } from 'react';
import { filterCards } from '../lib/filter.js';
import { maxCopies } from '../lib/deck.js';
import { cardName, cardImageSrc, cardThumbSrc } from '../lib/lang.js';
import { useCardPreview, CardPreview } from './CardPreview.jsx';
import { useT } from '../i18n.jsx';

const CAP = 600; // safety cap on rendered cells

export default function CardBrowser({ cards, filters, quantities, lang, onChangeQty, onToggle, onSelectAll }) {
  const t = useT();
  const filtered = useMemo(() => filterCards(cards, filters), [cards, filters]);
  const shown = filtered.slice(0, CAP);
  const { previewRef, previewImgRef, trackPointer, hidePreview } = useCardPreview(lang);

  return (
    <div className="browser">
      <div className="browser-meta">
        <span>
          {t('browser.count', { n: filtered.length })}{filtered.length > CAP ? t('browser.capped', { cap: CAP }) : ''}
        </span>
        {filtered.length > 0 && (
          <button className="btn secondary small" onClick={() => onSelectAll(filtered.map((c) => c.id))}>
            {t('browser.selectAll', { n: filtered.length })}
          </button>
        )}
      </div>
      <div className="grid">
        {shown.map((c) => {
          const qty = quantities[c.id] || 0;
          const max = maxCopies(c);
          const name = cardName(c, lang);
          return (
            <div key={c.id} className={`cardcell ${qty > 0 ? 'selected' : ''}`}>
              {/* Click image to select (qty 1) / deselect. Use −/+ for copies once selected.
                  Hover shows a full-size preview. */}
              <img
                src={cardThumbSrc(c, lang)}
                alt={name}
                loading="lazy"
                onClick={() => onToggle(c.id)}
                onMouseEnter={(e) => trackPointer(e, c)}
                onMouseMove={(e) => trackPointer(e, c)}
                onMouseLeave={hidePreview}
                onError={(e) => {
                  // Missing localized thumb → English thumb → direct full-res
                  // (the last step also covers the proxy being unavailable).
                  const el = e.currentTarget;
                  const chain = [...new Set([cardThumbSrc(c, lang), cardThumbSrc(c, 'en'), cardImageSrc(c, 'en')].filter(Boolean))];
                  const next = chain[chain.indexOf(el.getAttribute('src')) + 1];
                  if (next) el.src = next;
                }}
              />
              {qty > 0 && (
                <div className="qty-ctrl">
                  <button className="qty-btn" onClick={() => onChangeQty(c.id, +1)} disabled={qty >= max} aria-label={t('browser.addCopy')}>+</button>
                  <span className="qty-count">{qty}</span>
                  <button className="qty-btn" onClick={() => onChangeQty(c.id, -1)} aria-label={t('browser.removeCopy')}>−</button>
                </div>
              )}
            </div>
          );
        })}
      </div>
      {/* Shared hover preview (hidden until a card is hovered). */}
      <CardPreview previewRef={previewRef} previewImgRef={previewImgRef} />
    </div>
  );
}
```

- [ ] **Step 3: Vérifier la non-régression (suite Vitest)**

Run: `npm test`
Expected: PASS (aucun test ne cible ces composants ; on vérifie surtout que rien n'est cassé côté `lib/`).

- [ ] **Step 4: Vérifier manuellement la grille**

Run: `npm run dev` puis ouvrir http://localhost:5173.
Vérifier, à l'identique d'avant :
- Survol d'une carte de la grille : la preview plein format apparaît après ~600 ms, suit le curseur, bascule à gauche près du bord droit.
- Scroller la grille pendant un survol : la preview disparaît immédiatement.
- Quitter la carte : la preview disparaît.

- [ ] **Step 5: Commit**

```bash
git add web/src/components/CardPreview.jsx web/src/components/CardBrowser.jsx
git commit -m "refactor: extract shared card hover preview into CardPreview module"
```

---

### Task 3: Volet deck — miniatures + survol plein format

Remplace le plein format par des miniatures wsrv.nl (largeur suivant le zoom via `deckThumbWidth`) et ajoute le survol plein format partagé.

**Files:**
- Modify: `web/src/components/DeckPanel.jsx`

- [ ] **Step 1: Mettre à jour les imports de `DeckPanel.jsx`**

Remplacer la ligne d'import de `lang.js` (ligne 2) et ajouter l'import du module preview. En tête de fichier :

```jsx
import React, { useState } from 'react';
import { cardName, cardThumbSrc, cardImageEn, deckThumbWidth } from '../lib/lang.js';
import { maxCopies } from '../lib/deck.js';
import { useCardPreview, CardPreview } from './CardPreview.jsx';
import { useT } from '../i18n.jsx';
```

- [ ] **Step 2: Réécrire `MiniCard` (miniature + handlers de survol)**

Remplacer la fonction `MiniCard` entière par :

```jsx
// One selected card, shown as a compact version of a browser grid cell: same
// thumbnail + same −/count/+ control. Clicking the image asks for confirmation
// before removing the card, so a stray click can't silently empty the deck.
// Hover shows the shared full-size preview so the card stays readable at any zoom.
function MiniCard({ card, qty, lang, thumbW, onChangeQty, onToggle, trackPointer, hidePreview }) {
  const t = useT();
  const [confirming, setConfirming] = useState(false);
  const max = maxCopies(card);
  const name = cardName(card, lang);
  return (
    <div className="cardcell deck-mini selected" title={name}>
      <img
        src={cardThumbSrc(card, lang, thumbW)}
        alt={name}
        loading="lazy"
        onClick={() => setConfirming(true)}
        onMouseEnter={(e) => trackPointer(e, card)}
        onMouseMove={(e) => trackPointer(e, card)}
        onMouseLeave={hidePreview}
        onError={(e) => {
          // Missing localized thumb → English thumb → full-res English
          // (also covers the proxy being unavailable).
          const el = e.currentTarget;
          const chain = [...new Set([cardThumbSrc(card, lang, thumbW), cardThumbSrc(card, 'en', thumbW), cardImageEn(card)].filter(Boolean))];
          const next = chain[chain.indexOf(el.getAttribute('src')) + 1];
          if (next) el.src = next;
        }}
      />
      {confirming ? (
        <div className="deck-mini-confirm">
          <button
            className="btn danger small"
            onClick={() => { setConfirming(false); onToggle(card.id); }}
          >{t('panel.remove')}</button>
          <button
            className="btn secondary small"
            onClick={() => setConfirming(false)}
          >{t('common.cancel')}</button>
        </div>
      ) : (
        <div className="qty-ctrl">
          <button
            className="qty-btn"
            onClick={() => onChangeQty(card.id, +1)}
            disabled={qty >= max}
            aria-label={t('browser.addCopy')}
          >+</button>
          <span className="qty-count">{qty}</span>
          <button
            className="qty-btn"
            onClick={() => onChangeQty(card.id, -1)}
            aria-label={t('browser.removeCopy')}
          >−</button>
        </div>
      )}
    </div>
  );
}
```

- [ ] **Step 3: Instancier le hook + calculer `thumbW` dans `DeckPanel`**

Dans le corps de `DeckPanel`, juste après `const t = useT();` (ligne 90), ajouter l'appel du hook (il DOIT être inconditionnel, avant le `if (collapsed)` plus bas) :

```jsx
  const t = useT();
  const { previewRef, previewImgRef, trackPointer, hidePreview } = useCardPreview(lang);
```

Puis, juste après le calcul de `cardW` (ligne 94), ajouter :

```jsx
  const cardW = Math.round((SOURCE_WIDTH * zoom) / 100);
  const thumbW = deckThumbWidth(cardW);
  const gridStyle = { gridTemplateColumns: `repeat(auto-fill, minmax(min(${cardW}px, 100%), ${cardW}px))` };
```

- [ ] **Step 4: Passer les nouvelles props à chaque `MiniCard` + rendre `<CardPreview>`**

Dans le `.map` du corps (autour de la ligne 188), remplacer le `<MiniCard …/>` par :

```jsx
                {g.items.map(({ card, qty }) => (
                  <MiniCard
                    key={card.id}
                    card={card}
                    qty={qty}
                    lang={lang}
                    thumbW={thumbW}
                    onChangeQty={onChangeQty}
                    onToggle={onToggle}
                    trackPointer={trackPointer}
                    hidePreview={hidePreview}
                  />
                ))}
```

Et, juste avant la balise fermante `</div>` du conteneur `deckpanel` (fin du `return` déplié, après `deckpanel-body`), ajouter l'élément preview partagé :

```jsx
      </div>
      {/* Shared hover preview (hidden until a card is hovered). */}
      <CardPreview previewRef={previewRef} previewImgRef={previewImgRef} />
    </div>
  );
}
```

> Le retour anticipé `if (collapsed)` (volet replié) ne rend pas de preview — c'est voulu, aucune carte n'y est affichée. Le hook, lui, est appelé avant ce retour, donc l'ordre des hooks reste stable.

- [ ] **Step 5: Lancer les tests**

Run: `npm test`
Expected: PASS.

- [ ] **Step 6: Vérifier manuellement le volet deck**

Run: `npm run dev` → http://localhost:5173. Sélectionner ~10 cartes pour ouvrir le volet, puis (onglet Réseau des devtools ouvert) :
- Les images du volet sont chargées via `wsrv.nl` en WebP (et non le CDN plein format).
- Bouger le zoom du volet à 100 % : une variante 570 px se charge, image nette.
- Survoler une carte du volet : la preview plein format apparaît après ~600 ms, lisible ; scroller l'annule.
- Cliquer une carte du volet : la confirmation de retrait inline s'affiche toujours (inchangé).

- [ ] **Step 7: Commit**

```bash
git add web/src/components/DeckPanel.jsx
git commit -m "perf: deck panel uses wsrv thumbnails + shared full-res hover preview"
```

---

### Task 4: Confirmation avant suppression d'un deck

**Files:**
- Modify: `web/src/components/DeckManager.jsx`
- Modify: `web/src/lib/i18n.js` (clé `decks.confirmDelete`, fr + en)

- [ ] **Step 1: Ajouter la clé i18n dans les DEUX langues**

Dans `web/src/lib/i18n.js`, bloc **fr**, juste après `'decks.delete': 'Suppr.',` (ligne 77) :

```js
    'decks.confirmDelete': 'Supprimer « {name} » ?',
```

Dans le bloc **en**, juste après `'decks.delete': 'Delete',` (ligne 189) :

```js
    'decks.confirmDelete': 'Delete “{name}”?',
```

- [ ] **Step 2: Vérifier la parité i18n**

Run: `npx vitest run test/i18n.test.js`
Expected: PASS — le test « keeps the fr and en dictionaries in sync » valide la présence de la clé des deux côtés.

- [ ] **Step 3: Ajouter l'état de confirmation**

Dans `web/src/components/DeckManager.jsx`, après `const [error, setError] = useState(null);` (ligne 10) :

```jsx
  const [confirmId, setConfirmId] = useState(null); // deck id awaiting delete confirmation
```

- [ ] **Step 4: Réinitialiser l'état après suppression effective**

Remplacer la fonction `remove` (lignes 43-46) par :

```jsx
  async function remove(id) {
    await api.deleteDeck(id);
    setConfirmId(null);
    await refresh();
  }
```

- [ ] **Step 5: Rendre la confirmation inline dans la ligne**

Remplacer le contenu du `<li>` de la liste des decks (lignes 67-72) par :

```jsx
            <li key={d.id}>
              <span className="name">{d.name} <span className="muted">· {t('decks.cardsCount', { n: d.count })}</span></span>
              {confirmId === d.id ? (
                <>
                  <span className="muted">{t('decks.confirmDelete', { name: d.name })}</span>
                  <button className="btn danger small" onClick={() => remove(d.id)}>{t('panel.remove')}</button>
                  <button className="btn secondary small" onClick={() => setConfirmId(null)}>{t('common.cancel')}</button>
                </>
              ) : (
                <>
                  <button className="btn secondary" onClick={() => load(d.id)}>{t('decks.load')}</button>
                  <button className="btn secondary" onClick={() => duplicate(d.id)}>{t('decks.duplicate')}</button>
                  <button className="btn secondary" onClick={() => setConfirmId(d.id)}>{t('decks.delete')}</button>
                </>
              )}
            </li>
```

- [ ] **Step 6: Vérifier manuellement**

Run: `npm run dev` → ouvrir « Mes decks » (bouton Gérer). Pour un deck sauvegardé :
- Clic « Suppr. » → la ligne affiche « Supprimer « … » ? » + boutons Supprimer / Annuler, sans rien effacer.
- Annuler → retour aux boutons Charger/Dupliquer/Suppr., deck intact.
- Suppr. (confirmé) → le deck disparaît de la liste.
- Ouvrir la confirmation d'un deck A puis cliquer « Suppr. » d'un deck B → la confirmation bascule sur B (une seule à la fois).

- [ ] **Step 7: Commit**

```bash
git add web/src/components/DeckManager.jsx web/src/lib/i18n.js
git commit -m "fix: confirm before deleting a saved deck"
```

---

### Task 5: Gestion d'erreur à l'upload d'un dos de carte

**Files:**
- Modify: `web/src/components/ExportDialog.jsx`
- Modify: `web/src/lib/i18n.js` (clé `export.back.uploadError`, fr + en)

- [ ] **Step 1: Ajouter la clé i18n dans les DEUX langues**

Dans `web/src/lib/i18n.js`, bloc **fr**, juste après `'export.back.none': 'Aucun dos sélectionné',` (ligne 111) :

```js
    'export.back.uploadError': 'Image illisible — choisis un autre fichier.',
```

Dans le bloc **en**, juste après `'export.back.none': 'No back selected',` (ligne 221) :

```js
    'export.back.uploadError': 'Unreadable image — pick another file.',
```

- [ ] **Step 2: Vérifier la parité i18n**

Run: `npx vitest run test/i18n.test.js`
Expected: PASS.

- [ ] **Step 3: Envelopper `pickBack` dans try/catch**

Dans `web/src/components/ExportDialog.jsx`, remplacer la fonction `pickBack` (lignes 46-52) par :

```jsx
  async function pickBack(group, file) {
    if (!file) return;
    setError(null);
    try {
      const { path } = await api.uploadBack(file);
      const next = { ...backs, [group]: path };
      setBacks(next);
      onBacksChange(next);
    } catch {
      // accept="image/*" is only advisory: an SVG, a corrupt file, or a renamed
      // non-image passes it and makes createImageBitmap reject. Surface it.
      setError(t('export.back.uploadError'));
    }
  }
```

> Le message s'affiche via le bloc `{error && …}` déjà présent (ligne 171). En cas d'échec, `backs` n'est pas modifié : le dos courant reste en place.

- [ ] **Step 4: Vérifier les tests**

Run: `npm test`
Expected: PASS.

- [ ] **Step 5: Vérifier manuellement**

Run: `npm run dev` → ouvrir Export, format MPC ou PDF (les sélecteurs de dos apparaissent). Cliquer « Choisir un dos » et sélectionner un fichier non-image (renommer un `.txt` en `.png`, ou choisir un `.svg`) :
- Un message d'erreur « Image illisible… » s'affiche.
- L'état du dos (« par défaut » / « personnalisé ») n'a pas changé.
- Choisir ensuite une vraie image (PNG/JPEG) fonctionne et efface l'erreur.

- [ ] **Step 6: Commit**

```bash
git add web/src/components/ExportDialog.jsx web/src/lib/i18n.js
git commit -m "fix: handle unreadable image on custom card-back upload"
```

---

### Task 6: Vérification finale

**Files:** aucun (validation seulement)

- [ ] **Step 1: Suite complète**

Run: `npm test`
Expected: PASS (tous les fichiers, dont `lang` et `i18n`).

- [ ] **Step 2: Build de production**

Run: `npm run build`
Expected: succès, `web/dist` régénéré sans erreur.

- [ ] **Step 3: E2E manuel sur le build**

Run: `npm run preview` → parcourir : filtrer → sélectionner ~10 cartes → volet deck (miniatures + survol) → sauvegarder un deck → le supprimer avec confirmation → Export → tester un upload de dos invalide puis valide → générer un ZIP. Tout fonctionne.

---

## Self-review

**Couverture de la spec :**
- §1 confirmation suppression → Task 4. ✓
- §2a miniatures zoom-scaled (`deckThumbWidth`) → Task 1 + Task 3 (steps 2-3). ✓
- §2b survol plein format extrait + réutilisé → Task 2 (extraction + CardBrowser) + Task 3 (DeckPanel). ✓
- §3 erreur upload dos → Task 5. ✓
- Tests : `deckThumbWidth` (Task 1), parité i18n (Tasks 4-5), vérifs manuelles (Tasks 2,3,4,5,6). ✓

**Cohérence des types/signatures :**
- `deckThumbWidth(cardW) → number`, défini Task 1, consommé Task 3 (`thumbW = deckThumbWidth(cardW)`). ✓
- `useCardPreview(lang) → { previewRef, previewImgRef, trackPointer, hidePreview }`, défini Task 2, consommé Tasks 2 (CardBrowser) et 3 (DeckPanel). ✓
- `<CardPreview previewRef previewImgRef />`, défini Task 2, rendu Tasks 2 et 3. ✓
- `cardThumbSrc(card, lang, w)` : signature existante (déjà testée), 3ᵉ arg `w` = `thumbW` en Task 3. ✓
- Clés i18n `decks.confirmDelete` (param `{name}`) et `export.back.uploadError` : ajoutées fr+en, consommées Tasks 4 et 5. ✓

**Placeholders :** aucun — chaque step de code montre le code complet.

**Note :** Task 2 réécrit intégralement `CardBrowser.jsx` ; l'ancien fichier importait `cardImageEn` et `useRef/useEffect` désormais inutiles dans ce composant (déplacés dans `CardPreview.jsx`) — le remplacement complet fourni les retire déjà.
